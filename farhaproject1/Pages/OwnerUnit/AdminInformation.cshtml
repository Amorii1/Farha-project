@page
@model farhaproject1.Pages.OwnerUnit.AdminInformationModel
@{
    ViewData["Title"] = "AdminInformation";
}



    <div class="float-right pt-5 pr-5" >
        <div class="float-right pt-5 pr-5"> <a asp-area="Identity" asp-page="/Account/Register" class="btn btn-primary">Add new Admin</a></div>
       
    </div>
    @*<div class=" pt-5 pr-5">
        <div class="card border-dark mb-3" style="max-width: 20rem;">
            <div class="card-header">Header</div>
            <div class="card-body">
                <h4 class="card-title">Dark card title</h4>
                <p class="card-text">Some quick example text to build on the card title and make up the bulk of the card's content.</p>
            </div>
        </div>*@
        <div>
            @if (Model.InformationOfAdmin.Count() == 0)
            {
                <p>No Admin has been added! Add new Amin</p>
            }
            else
            {
                <table class="table table-striped  border-info border">
                    <tr class="table-secondary">

                        <th>
                            @Html.DisplayNameFor(m => m.InformationOfAdmin[0].Email)
                        </th>
                        <th>
                            @Html.DisplayNameFor(m => m.InformationOfAdmin[0].PhoneNumber)
                        </th>
                        <th>
                            @Html.DisplayNameFor(m => m.InformationOfAdmin[0].CenterName)
                        </th>
                        <th>
                            @Html.DisplayNameFor(m => m.InformationOfAdmin[0].CenterLocation)
                        </th>

                        <th></th>
                        <th></th>
                    </tr>
                    @foreach (var item in Model.InformationOfAdmin)
                    {
                <tr>

                    <td class="text-secondary">
                        @Html.DisplayFor(m => item.PhoneNumber)
                    </td>
                    <td class="text-secondary">
                        @Html.DisplayFor(m => item.Email)
                    </td>
                    <td class="text-secondary">
                        @Html.DisplayFor(m => item.CenterName)
                    </td>
                    <td class="text-secondary">
                        @Html.DisplayFor(m => item.CenterLocation)
                    </td>
                    <td></td>
                    <td style="width:150px;">
                        <div class="btn-group" role="group">
                            <a class="btn btn-primary" asp-page="EditAdminInformation" asp-route-Id="@item.Id" title="Edit"><i class="fas fa-edit"></i>Edit</a>

                            <a class="btn btn-secondary" asp-page="DeleteAdminInformation" asp-route-Id="@item.Id" title="Delete"><i class="far fa-trash-alt"></i>Delete</a>


                        </div>

                    </td>
                </tr>

                    }
                </table>
            }
        </div>
